apiVersion: 1

providers:
  - name: "DNS Monitor dashboards"
    folder: ""                          # Root folder in the Grafana UI
    type: file
    options:
      path: /var/lib/grafana/dashboards # Where to look inside the container